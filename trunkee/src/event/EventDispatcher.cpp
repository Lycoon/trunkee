#include "EventDispatcher.h"

/*
template <typename EventData>
void EventDispatcher<EventData>::Dispatch(const EventData& e)
{
	for (auto& listener : m_listeners)
		listener(e);
}

template <typename EventData>
void EventDispatcher<EventData>::AddListener(Listener<EventData> listener)
{
	m_listeners.push_back(listener);
}
*/